provider:
  name: aws
  runtime: nodejs14.x
  environment:  
    userDetails: ${self:custom.userDetails}
functions:
  tests3:
    handler: handler.main
    events:
      - httpApi:
          path: /
          method: get
